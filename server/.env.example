# Server configuration
PORT=5000
NODE_ENV=development

# Local MySQL configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=autosar_db

# Optional: without a key the application generates a local template-based SAR.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
